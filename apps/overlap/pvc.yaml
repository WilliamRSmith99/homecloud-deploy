apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overlap-data
  namespace: overlap
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
