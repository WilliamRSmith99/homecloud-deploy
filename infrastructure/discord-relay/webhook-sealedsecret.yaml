# Placeholder — this SealedSecret has not been sealed yet. Generate it
# against the live cluster's sealed-secrets controller before this
# kustomization will build or the relay will sync with nothing to post to
# (see docs/deploying_a_new_sealed_secret.md). The key must be
# DISCORD_WEBHOOK — deployment.yaml pulls it in via envFrom, so the secret
# key name becomes the container's env var name directly:
#
#   kubectl create secret generic discord-webhook \
#     --namespace monitoring \
#     --from-literal=DISCORD_WEBHOOK='https://discord.com/api/webhooks/...' \
#     --dry-run=client -o yaml \
#   | kubeseal --format yaml \
#       --controller-namespace kube-system \
#       --controller-name sealed-secrets-controller \
#   > infrastructure/discord-relay/webhook-sealedsecret.yaml
