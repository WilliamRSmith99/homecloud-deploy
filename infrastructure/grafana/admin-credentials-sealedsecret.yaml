# Placeholder — this SealedSecret has not been sealed yet. Generate it
# against the live cluster's sealed-secrets controller before this
# kustomization will build or Grafana will sync (see
# docs/deploying_a_new_sealed_secret.md):
#
#   kubectl create secret generic grafana-admin-credentials \
#     --namespace monitoring \
#     --from-literal=admin-user='admin' \
#     --from-literal=admin-password='<choose-a-password>' \
#     --dry-run=client -o yaml \
#   | kubeseal --format yaml \
#       --controller-namespace kube-system \
#       --controller-name sealed-secrets-controller \
#   > infrastructure/grafana/admin-credentials-sealedsecret.yaml
