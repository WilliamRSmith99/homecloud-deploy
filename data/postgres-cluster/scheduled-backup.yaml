apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: homelab-pg-backup
spec:
  schedule: "0 4 * * *"
  cluster:
    name: homelab-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
